# Inventory Frontend (Material-UI)

This is a React frontend (Material-UI) for the Inventory backend.

## Install & run

```bash
npm install
npm start
```

Backend must run at http://localhost:8080 or set REACT_APP_API_URL.
